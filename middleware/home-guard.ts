import { PATH_ROUTER } from '~/shared/constants/router';

export default defineNuxtRouteMiddleware(async (to, from) => {
  // const isLogin = localStorage.getItem('token');
  // if (!isLogin) {
  //   return await navigateTo(PATH_ROUTER.login);
  // }
});
